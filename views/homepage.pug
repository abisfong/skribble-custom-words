extends base
block content
  .c-container.c-container--m-top.c-container--center-third
    if user
      include wordForm
    else
      include auth
    if words.length > 0
      include list
    else
      include nolist
    include custom-words
